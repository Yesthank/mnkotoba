rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 자기 데이터만 읽고 씁니다. 다른 사람 uid 경로는 전부 막힙니다.
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
